# Responsive Agent Reference

When you need to set responsive classes or inline styles, use `@trunkjs/responsive` inside `<tj-responsive>` with `-md:x` (`< md`), `md:x` (`>= md`), `md-xl:x` (`>= md && < xl`) or `style-md="..."`; for anything unclear, read [`.ai-usage-info.md`](.ai-usage-info.md).
