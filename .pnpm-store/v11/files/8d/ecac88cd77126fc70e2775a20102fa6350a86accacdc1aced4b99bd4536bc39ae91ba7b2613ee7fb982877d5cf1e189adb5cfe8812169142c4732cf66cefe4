export declare enum PeekType {
    /**
     * Include the match in the result
     * This will position the index after the match
     */
    Include = 0,
    /**
     * Exclude the match from the result
     * This will position the index after the match
     */
    Exclude = 1,
    /**
     * Peek without consuming the match
     * Index stays at the start of the match
        */
    Peek = 2
}
export type PrimitiveResult = {
    value: string;
    delimiter: string | null;
    isMultiline?: boolean;
};
type LanguageConfig = {
    booleanLiterals?: string[];
    numberPattern?: string;
    stringDelimiters?: string[];
    escapeCharacter?: string;
};
export declare class TokenReader2 {
    private _string;
    private _index;
    private _curLine;
    private _curColumn;
    /**
     * Returns the rest of the string from the current index.
     */
    get rest(): string;
    constructor(string: string);
    get curLine(): number;
    get curColumn(): number;
    get index(): number;
    get string(): string;
    get length(): number;
    isEnd(): boolean;
    hasMore(): boolean;
    readWhiteSpace(): string;
    private buildRegex;
    peek(peek: number | string | string[] | RegExp): string | null;
    private escapeRegExp;
    /**
     * Will read until the first occurrence of the peek string or regex.
     *
     * Will set the pointer to the position after the match (if type is Exclude or Include) or before the match (if type is Peek).
     *
     * @param peek
     * @param type
     */
    readUntil(peek: string | string[] | RegExp, type?: PeekType): {
        content: string;
        match: string | null;
    };
    private triggerError;
    readPrimitive(options?: LanguageConfig): PrimitiveResult;
    read(num?: number): string;
}
export {};
