version: 1.0

environments:
  coder:
    files:
      - name: 'project files'
        include:
          - './*.(js|html|css|ts|scss)'

        instructions: |
          These are all project files. You can edit them as you like.
